# NEPA-HRI API Environment Variables
# Copy to .env and fill in your values. Never commit .env.

# Comma-separated list of valid API keys
# Generate with: python -c "import uuid; print('npa_live_' + uuid.uuid4().hex[:24])"
NEPA_API_KEYS=npa_live_yourkey1,npa_live_yourkey2
